[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "chromium-chromedriver"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium",
  "python -m playwright install-deps"
]

[start]
cmd = "python smokey_bias_bot.py"
